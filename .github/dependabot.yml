version: 2
updates:
  # All Docker‑Compose services
  - package-ecosystem: "docker-compose"
    directories:
      - "/cloudflared"
      - "/nginx"
      - "/tsdproxy"
      - "/speedtest-tracker"
      - "/device-tracker"
      - "/influxdb"
      - "/pingvin"
      - "/grafana"
      - "/prometheus"
      - "/cadvisor"
    schedule:
      interval: "monthly"
